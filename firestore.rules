
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow open access to get the app working.
    // We will implement secure rules in a later step.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
