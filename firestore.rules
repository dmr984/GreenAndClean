
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    //
    // Non-production rules, will be changed before go-live
    //
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

    