'use client';

// This page now acts as a simple entry point for the /dashboard route.
// The actual content (Admin or Operator dashboard) is rendered by the DashboardLayout.
export default function DashboardPage() {
  return null;
}
